# CLAUDE.md — myprojectKids（幼児向け学習ページ・個人用）

このファイルは Claude Code がセッション開始時に読む規約。**myprojectP（雑貨店の商品管理システム）とは別のプロジェクト**で、あちらの台帳・フック・DB・NAS の共有物には一切触れない。

---

## 1. 何を作るか

- **誰が使うか**: ２人の子供（上の子 4歳・下の子 2歳）／**どの端末で**: iPad Air 2・横（iPadOS 15）／**どこで動かすか**: GitHub Pages（URL は `docs/spec.md` §1）
- **できること（初版）**: 画面5枚（詳しくは `docs/spec.md`）
  1. ひょう — 46字の あいうえお表・はなまるの印
  2. え と ことば — 絵と単語を見て、押すと声で聞く
  3. ことば — 絵なしで単語を読む
  4. なぞる — 指で字をなぞって はなまる
  5. おとなの ひとへ — 使い方と知らせること
- **やらないこと（初版）**: 濁音・拗音・カタカナ・書き順／子ども別の記録・端末間の同期／ログイン・DB・サーバの処理／縦向きの配置（全部は `docs/spec.md` §9）
- **草案の正本**: `docs/draft/`（別作業環境Cowork からの写し・ここでは編集しない・**公開リポジトリなので追跡しない**＝`.gitignore`）／**仕様の正本**: `docs/spec.md`（このプロジェクトで決めたことだけ）

## 2. スタックと置き場

- **スタック**: 静的 HTML＋CSS＋JS のみ（iPadOS 15 の Safari で動く書き方）——**DB は初版では置かない**（要るときは §1 の「できること」に理由を書いてから）
- **置き場**: `site/`（配信するもの）・`docs/`（仕様・記録）・`tests/`（機械チェック）・`.github/workflows/`（配信）
- **動かし方（1コマンド）**: リポジトリの直下で `python3 -m http.server 8080`（手元で見る）。配信は `main` に push すると GitHub Actions が Pages に上げる（リポジトリを公開にしたときから）
- **確認の仕方**: Mac の Safari で `http://localhost:8080/site/`。iPad で手元の版を見るなら `http://<Mac の名前>.local:8080/site/`（http なのでオフラインの仕組みは動かない）。配信した版は iPad のホーム画面のアイコンから
- **機械チェック**: `node tests/check.mjs`（NG が0件になるまで直す）。なぞりの数値は `http://localhost:8080/tests/trace-measure.html`

## 3. 進め方（myprojectP から持ち込む「分け方」だけ）

1. **仕様 → 実装 → 実機で確認**の順。仕様は `docs/spec.md` に1画面1節で書き、**書いてから作る**（作りながら仕様を変えたら、同じ commit で仕様も直す）。
2. **commit と push をする人は1本のセッションだけ**（「Kids Build」）。ほかのセッションを立てたときも、そちらは読むだけ・提案は `docs/review.md` に書く（`git add`・`git commit`・`git push` をしない）。
3. **参照先を変えたら消費者を全数で洗う**（ファイル名・id・クラス名・URL を変えたら `grep -rn '<変えた文字列>' site/ docs/` で0件まで直す）。
4. **失敗を無言にしない**（読み込みに失敗したら画面に一言出す・子どもが「動かない」と言わなくても分かるように）。
5. **記録は `docs/log.md` に日付と1行**（台帳の代わり。何を変えたか・次に何をするか）。

## 4. 画面の規約（子ども向けに効くものだけ）

- **インライン `style=` を書かない**（CSS はファイルへ）／**JS は外部ファイル**（HTML に書かない）。
- **タップの的は 44px 以上**・**文字は大きく**（本文 20px 以上を既定）・**色だけで意味を伝えない**（形か音も添える）。
- **外部リンク・広告・個人情報の入力欄を置かない**。子どもの名前・写真・生年月日は**リポジトリに入れない**（**リポジトリは公開**。設定ファイルやメモは `docs/local/` に置き `.gitignore`）。
- **音は「押したら鳴る」だけ**（自動再生しない）／**アニメは短く**（0.3秒以内・`prefers-reduced-motion` を尊重）。
- **オフラインでも開けるように**（外部 CDN を使わない・フォントも同梱）。

## 5. やらないこと

- `myprojectP/` の下のファイル・DB・NAS の共有フォルダ・Tailscale の設定を読まない・書かない。
- 部品が欲しければ**コピーして持ち込む**（リンクや共有はしない）。
- **GitHub のアカウントも別にする**: このリポジトリは、ユーザーが Google アカウントで作った専用のアカウントのもの。別システムのアカウントを使わない。gh の既定アカウントは切り替えない（別システムに干渉するので、このリポジトリの設定だけで分ける）。
- ロールを増やさない（2本目は「独立した目で見てほしい」と決めたときだけ・名前は「Kids Review」）。

---

# Compact instructions

When you are using compact, start the summary with a block titled 「セッション状態」 containing exactly these three lines, each present even when the answer is 「なし」:
1. `セッション: <Kids Build／Kids Review>`
2. `着手中: <docs/spec.md の節名・段階（仕様／実装／実機確認）>`
3. `未コミットの変更: <あり（ファイル名）／なし>`
